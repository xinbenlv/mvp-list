# Critic — Phase 5 (scaffold only)

TODO when Phase 5 ships — score each `PlanResult` on the 5 dims:

1. Stranger test — would a stranger reading the plan recognise it as personal?
2. Arc coherence — do the 4 stops form one day or 4 errands?
3. Specificity — does `why_fits_today` quote real intake fields?
4. Avoidance respect — does any stop violate `IntakeState.avoidance`?
5. Branch quality — is the `🔀 如果今天有变化` branch realistic?
